id: axis_max_lfq_secure

inherits: base.yaml

metadata:
  variant: Secure
  reference_docx: "reference/GCL Secure_LFQ.docx"

overrides:

  page: 1

  sections:

    - id: payment_details

      add_fields:
        - id: rider_sum_assured
          label: Rider Sum Assured
          datatype: number
          widget: textbox
          ocr: { engine: paddle }
          validation: { validator: amount }
