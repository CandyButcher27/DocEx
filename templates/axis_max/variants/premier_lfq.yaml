id: axis_max_lfq_premier

inherits: base.yaml

metadata:
  variant: Premier
  reference_docx: "reference/GCL Premier_LFQ 1.docx"

overrides:

  page: 1

  sections:

    - id: payment_details

      add_fields:
        - id: optional_atpd_sum_assured
          label: Optional ATPD Sum Assured
          datatype: number
          widget: textbox
          ocr: { engine: paddle }
          validation: { validator: amount }
